<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('homepage') }}">Shopping Cart</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">All Products</a></li>
                <li><a href="{{ path('add_product') }}">Add New Product</a></li>
                <li><a href="{{ path('display_my_cart') }}">Display My Shopping Cart</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="{{ path('display_my_cart') }}">
                        <i class="fa fa-shopping-cart fa-lg" aria-hidden="true"></i>Cart &nbsp;
                        {% if totalCountOfItems is defined %}
                            <span id="item-count" class="badge">{{ totalCountOfItems }}</span>
                        {% else %}
                            <span id="item-count" class="badge">0</span>
                        {% endif %}
                    </a>
                </li>
                <li>
                    <a id="empty-cart" href="{{ path('empty_my_cart') }}">Empty Cart</a>
                </li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>